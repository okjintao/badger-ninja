name: Deploy

on:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  cron:
    name: Inspect the Den
    runs-on: ubuntu-latest
    steps:
      - name: Call Deploy Hook
        run: curl ${{ secrets.NINJA_DEPLOY_HOOK }}
